{
  "name": "hn-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/style.css --out-file style.user.css",
    "build:safari": "parcel build src/style.css --out-file style.safari.css",
    "build:user": "parcel build src/style.css --out-file style.user.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-wrapper": "^0.2.2",
    "postcss": "^7.0.27",
    "postcss-cssnext": "^3.1.0",
    "postcss-each": "^0.10.0",
    "postcss-nested": "^4.2.1"
  }
}
